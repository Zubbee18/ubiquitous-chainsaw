[phases.setup]
nixPkgs = ["nodejs_18", "python3", "gcc", "gnumake", "pkg-config"]

[phases.install]
cmds = ["npm install --legacy-peer-deps"]

[phases.build]
cmds = ["npm rebuild sqlite3 --build-from-source --target_arch=x64 --target_platform=linux"]

[start]
cmd = "npm start"
